# CLAUDE.md

This repo's guidance for AI coding tools lives in **[`AGENTS.md`](AGENTS.md)** —
read it first. It covers the docs reading order, the non-negotiable invariants
(outbound-only, no arbitrary command execution, opt-in default-OFF,
release-please owns the version), and the conventions.

Keep the rules in `AGENTS.md` only; this file is just a pointer so Claude Code
and Codex stay in sync.
